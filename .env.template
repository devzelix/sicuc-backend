# ===================================================================
# == APPLICATION ENVIRONMENT VARIABLES (RUNTIME & SECRETS)
# ===================================================================

# --- Application/Server Configuration ---

# Defines the base context path for the REST API (e.g., /api/v1). 
API_BASE_PATH=/api/v1

# Defines the active Spring profile (e.g., 'dev' or 'prod'). 
SPRING_PROFILES_ACTIVE=dev

# --- Database Management (Flyway/Hibernate) ---

# Hibernate DDL generation policy: 
# 'update' (dev only), 'validate' (prod only), or 'none' (when using Flyway).
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# Database connection URL (used by Spring).
SPRING_DATASOURCE_URL=jdbc:mysql://sicuc-db:3306/sicuc_db

# Database credentials (injected into Spring)
SPRING_DATASOURCE_USERNAME=sicuc_user
SPRING_DATASOURCE_PASSWORD=sicuc_password_dev

# MySQL root password (used by docker-compose to set up the DB container).
MYSQL_ROOT_PASSWORD_SECRET=my-strong-root-password

# --- JWT Security Configuration ---

# CRITICAL: Secret key used to sign and verify JWT tokens. Must be long and random.
JWT_SECRET_KEY=your-ultra-secure-base64-secret-key

# Access token expiration time in milliseconds (e.g., 3600000 = 1 hour).
JWT_ACCESS_TOKEN_EXPIRATION=3600000

# Refresh token expiration time in milliseconds (e.g., 604800000 = 7 days).
JWT_REFRESH_TOKEN_EXPIRATION=604800000